#!/usr/bin/env python
#
# Autogenerated by Thrift
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import ThriftFlumeClientServer
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host:port] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  bool heartbeat(string logicalNode, string physicalNode, string host, FlumeNodeState s, i64 timestamp)'
  print '  ThriftFlumeConfigData getConfig(string sourceId)'
  print '   getLogicalNodes(string physNode)'
  print '   getChokeMap(string physNode)'
  print '  void acknowledge(string ackid)'
  print '  bool checkAck(string ackid)'
  print '  void putReports( reports)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = ThriftFlumeClientServer.Client(protocol)
transport.open()

if cmd == 'heartbeat':
  if len(args) != 5:
    print 'heartbeat requires 5 args'
    sys.exit(1)
  pp.pprint(client.heartbeat(args[0],args[1],args[2],eval(args[3]),eval(args[4]),))

elif cmd == 'getConfig':
  if len(args) != 1:
    print 'getConfig requires 1 args'
    sys.exit(1)
  pp.pprint(client.getConfig(args[0],))

elif cmd == 'getLogicalNodes':
  if len(args) != 1:
    print 'getLogicalNodes requires 1 args'
    sys.exit(1)
  pp.pprint(client.getLogicalNodes(args[0],))

elif cmd == 'getChokeMap':
  if len(args) != 1:
    print 'getChokeMap requires 1 args'
    sys.exit(1)
  pp.pprint(client.getChokeMap(args[0],))

elif cmd == 'acknowledge':
  if len(args) != 1:
    print 'acknowledge requires 1 args'
    sys.exit(1)
  pp.pprint(client.acknowledge(args[0],))

elif cmd == 'checkAck':
  if len(args) != 1:
    print 'checkAck requires 1 args'
    sys.exit(1)
  pp.pprint(client.checkAck(args[0],))

elif cmd == 'putReports':
  if len(args) != 1:
    print 'putReports requires 1 args'
    sys.exit(1)
  pp.pprint(client.putReports(eval(args[0]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
